## Installation
### Requirements: `Python >= 3.9`
 At this stage of development, package available only in this private repository.
 You can install it in your Python (>= 3.9) environment by this command:
```bash
pip install git+ssh://git@github.com/ghotek/convolution.git
```
